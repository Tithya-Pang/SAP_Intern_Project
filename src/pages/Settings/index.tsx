import { BellOutlined,NumberOutlined,SafetyCertificateOutlined,TeamOutlined } from '@ant-design/icons';
import { Alert,Button,Card,Form,Input,InputNumber,Select,Space,Switch,Typography,message } from 'antd';
import { useEffect,useState } from 'react';
import { useApp } from '@/context/AppContext';
import { temporaryCreditService } from '@/services/temporaryCreditService';
import type { AppSettings } from '@/types/domain';
import { canManageSettings } from '@/utils/permissions';
export default function Settings(){const{currentUser,users}=useApp();const[form]=Form.useForm<AppSettings>();const[loading,setLoading]=useState(true);const[saved,setSaved]=useState(false);useEffect(()=>{void temporaryCreditService.getSettings().then(value=>form.setFieldsValue(value)).finally(()=>setLoading(false));},[]);if(!currentUser||!canManageSettings(currentUser.role))return<Alert type="error" showIcon message="Access denied" description="Administrator permission is required to manage settings."/>;const save=async()=>{const values=await form.validateFields();await temporaryCreditService.updateSettings(values,currentUser);setSaved(true);message.success('Settings saved for this session.');};const reset=async()=>{const values=await temporaryCreditService.resetSettings(currentUser);form.setFieldsValue(values);setSaved(false);message.info('Settings reset to defaults.');};return<div className="page"><div className="pageHeader"><div><Typography.Title level={2} className="pageTitle">Settings</Typography.Title><div className="pageSubtitle">Manage prototype users, rules, risk thresholds, and numbering.</div></div><Space><Button onClick={()=>void reset()}>Reset Defaults</Button><Button type="primary" onClick={()=>void save()}>Save Changes</Button></Space></div>{saved&&<Alert type="success" showIcon closable message="Settings updated for this session." style={{marginBottom:16}}/>}<Form form={form} layout="vertical" disabled={loading}><div className="settingsGrid"><Card title={<Space><TeamOutlined/>Users & Business Units</Space>} className="surface"><Form.Item label="Prototype users"><Select mode="multiple" value={users.map(u=>u.id)} options={users.map(u=>({value:u.id,label:`${u.name} · ${u.role.replaceAll('_',' ')}`}))}/></Form.Item><Form.Item name="businessUnits" label="Business units" rules={[{required:true}]}><Select mode="tags"/></Form.Item></Card><Card title={<Space><SafetyCertificateOutlined/>Risk Rules</Space>} className="surface"><Space><Form.Item name="lowRiskMax" label="Low-risk upper score" rules={[{required:true}]}><InputNumber min={0} max={100}/></Form.Item><Form.Item name="mediumRiskMax" label="Medium-risk upper score" rules={[{required:true}]}><InputNumber min={0} max={100}/></Form.Item></Space><Form.Item name="rejectionReasons" label="Rejection reasons" rules={[{required:true}]}><Select mode="tags"/></Form.Item></Card><Card title={<Space><NumberOutlined/>Request Numbering</Space>} className="surface"><Form.Item name="requestPrefix" label="Prefix" rules={[{required:true,pattern:/^[A-Z]{2,6}$/}]}><Input/></Form.Item><Form.Item name="nextSequence" label="Next sequence" rules={[{required:true}]}><InputNumber min={1} style={{width:'100%'}}/></Form.Item></Card><Card title={<Space><BellOutlined/>Notifications & Information</Space>} className="surface"><Form.Item name="notificationsEnabled" label="Enable mock notifications" valuePropName="checked"><Switch/></Form.Item><Form.Item name="informationTypes" label="Information request types" rules={[{required:true}]}><Select mode="tags"/></Form.Item></Card></div></Form></div>;}
